CocoaPods Curly Quotes bug
====================

Example project demonstrating a bug in CocoaPods. Curly quotes (”) are replaced with Äù when `pod install` is run. 

This project is accidentally named 'CurlyBraces' for no reason at all :)
